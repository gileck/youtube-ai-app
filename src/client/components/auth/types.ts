export interface LoginFormState {
    username: string;
    email: string;
    password: string;
    confirmPassword: string;
}

export interface LoginFormErrors {
    username: string;
    email: string;
    password: string;
    confirmPassword: string;
} 